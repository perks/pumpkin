val x : List[Int] = [1, 2, 3, 4]
val i = 2
[1]

x[1+i]

1::x

True::[]

val l = [1, 2, 3, 4]

def map (f : (Int => Int), l : List[Int]) : List[Int] =>
    val head = hd(l)
    val tail = tl(l)
    
    f(head)::map(f, tail)
