def gcd(a : Int, b : Int) : Int =>
	if a == 0:
		b
	else:
		if b == 0:
			a
		else:
			gcd(a, a%b)

val y = (a : Int => a : Int)
val z = (a : Int => a : Int)
val a = (a : Int => a : Int)

val p = gcd >> y << z << a
val n = gcd>> ((a >> z)>>y)
