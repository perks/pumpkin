val l = [1, 2, 3, 4]

def map(f : (Int => Int), l : List[Int]) : List[Int] =>
  if is_empty(l) :
    l
  else :
    hd(l)::map(f, tl(l))

val l2 = map((x : Int => x + 1), l)

print(l2[0])
print(l2[1])
print(l2[2])
print(l2[3])

